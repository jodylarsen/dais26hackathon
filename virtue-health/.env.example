DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_CONFIG_PROFILE=your_profile_name
DATABRICKS_WAREHOUSE_ID=your_warehouse_id
DATABRICKS_APP_PORT=8000
DATABRICKS_APP_NAME=virtue-health
FLASK_RUN_HOST=0.0.0.0

# ── Optional: Lakebase / PostgreSQL (only needed if using Lakebase synced tables)
PGDATABASE=your_postgres_database
LAKEBASE_ENDPOINT=your_postgres_endpoint_path
PGHOST=your_postgres_host
PGPORT=5432
PGSSLMODE=require

# ── Enrichment: data.gov.in (MCA21 company registry bulk export only)
# Pincode lookup is covered by api.postalpincode.in — no token needed
# data.gov.in requires Indian phone number for OTP verification
# DATA_GOV_IN_API_KEY=your_data_gov_in_api_key

# ── Enrichment: ABDM Health Facility Registry (HFR)
# Register at https://sandbox.abdm.gov.in/register — get client_id + client_secret
# Prod: https://www.ndhm.gov.in — requires NHA empanelment
ABDM_CLIENT_ID=your_abdm_client_id
ABDM_CLIENT_SECRET=your_abdm_client_secret

# ── Enrichment: Overpass API preferred mirror (primary overpass-api.de is rate-limited)
# Confirmed working: https://overpass.openstreetmap.fr/api/interpreter
OVERPASS_API_URL=https://overpass.openstreetmap.fr/api/interpreter

# ── Enrichment: GeoNames, Wikidata, Overture Maps — no token required (public)
